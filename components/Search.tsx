import React from 'react';
import {Text, SafeAreaView, StyleSheet} from 'react-native';

const Search = () => {
  return (
    <SafeAreaView>
      <Text style={styles.container}>SEARCH PLACEHOLDER</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  container: {
    color: 'black',
    display: 'flex',
  },
});

export default Search;
